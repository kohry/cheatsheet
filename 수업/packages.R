# 샘플링 패키지

## doBy는 그룹별로 뭔가 하고자 할때

install.packages("doBy")
library(doBy)
install.packages("sampling") # 더 나은 성능이 좋은 패키지임.ㅅㅁ
library(sampling)
install.packages("dplyr") 
library(dplyr)

install.packages("ggplot2")
library(ggplot2)

install.packages("MASS") 
library(MASS)

install.packages("car") # 굉장히 많은 기능들이 들어가이
library(car)

install.packages("outliers")
library(outliers)

install.packages("DMwR")
library(DMwR)

install.packages("Rlof") # lof
library(Rlof)

#1071 신경망 확장된 기법들임

install.packages("nnet") #multinomial
library(nnet)

install.packages("gmodels") # for cacluating cross table
library(gmodels)

install.packages("caret") # 민감도 특이도
library(caret)

install.packages("e1071") # 민감도 특이도
library(e1071)

install.packages("randomForest") # randomForest
library(randomForest)

install.packages("KoNLP")
library(KoNLP)

install.packages("hflights")
library(hflights)

install.packages("readr")
library(readr)
